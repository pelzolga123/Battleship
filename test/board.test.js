/* eslint-disable no-undef */

import Board from '../src/board';

describe('Board', () => {
  it('is a function', () => {
    expect(typeof Board).toBe('function');
  });
  it('is a function', () => {
    expect(typeof Board).toBe('function');
  });
});
